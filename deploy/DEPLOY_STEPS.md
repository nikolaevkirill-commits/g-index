# DEPLOY — що робити (проста інструкція)

Відкривай цей файл щоразу при оновленні. Роби по порядку.

---

## КРОК 1. Скачай файли (з чату)
Усі файли, що я дав — натисни і збережи на ПК.

## КРОК 2. На ПК — заміни 2 файли в папці проєкту
- `fetch_kp_v2.py` (замість старого `fetch_kp_v2__1_.py`)
- `update_kp.bat` (простий, без зайвих залежностей)

## КРОК 3. Запусти один раз
Подвійний клік на **`update_kp.bat`** (НЕ friday_routine — той вимагає файл, якого нема).
→ Зʼявиться новий файл `future_kp.json`.

## КРОК 4. Завантаж у папку `deploy/`
Перетягни туди:
- index.html
- sw.js
- expert_overrides_v3.json
- pdf48_ground_truth_v6.json
- future_kp.json
- _headers

## КРОК 5. Готово
Далі компʼютер усе робить сам щопонеділка.

---

## Що це дало
- Четвер і Пʼятниця тепер правильні (червоні де треба)
- Фейковий Kp зник — тепер реальний з NOAA
- Оновлюється саме, без тебе

---

## Якщо щось не працює
- Дашборд показує стару версію → натисни кнопку «Кеш» у дашборді
- `update_kp.bat` (простий, без зайвих залежностей) видає помилку → перевір, що Python встановлено
- Питання → нова сесія зі мною, дай цей файл + STATE.md

---

*Памʼятай: усі робочі файли — в outputs цього чату. Нічого не загубиться.*
